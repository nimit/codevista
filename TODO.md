<!-- UI -->
- dark mode by default
- the commenting UI does not match the rest of the plan UI (which looks polished) while the comment UI looks like plain, default HTML. bring it to consistency - color, spacing, border radius, etc
<!-- SKILL -->
- the open questions section isn't clickable. add an option to type a custom answer for each question at the end and make the options clickable. will need to probably add another functionality/endpoint to the server called answer_question. which answers the questions and either modifies the original plan and chooses one option as selected (i want this if possible) or as a last resort, creates another file called answers.json (similar to comments.json) with the answers.
- the content in plans created by this skill could be improved. I tried creating a plan using the brainstorming skill (inside superpower plugin) and then invoking codevista to display the plan. I really liked that plan. But, it led to token and time waste: superpower brainstorms and writes plan to a file -> codevista understands the skill and how to write a visual plan -> translates written plan to visual plan and displays. is there a way to git submodule that part of the superpowers plugin or as a last resort, we can just copy the guidelines and steps for plan making into this repository so the agent learns how to create plans but outputs them into the visual plan format consumed by the server.